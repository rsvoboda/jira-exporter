package io.quarkus.qe.metrics;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeMetricsEndpointIT extends MetricsEndpointTest {

    // Execute the same tests but in native mode.
}